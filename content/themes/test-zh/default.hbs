<!DOCTYPE html>
<html lang="zh-CN">
<head>
    {{! Document Settings }}
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    {{! Page Meta }}
    <title>{{meta_title}}</title>
    <meta name="keywords" content="日事清官方博客" />
    <meta name="description" content="官方博客向您展示产品的最新动态，活动沙龙，以及媒体报道和用户故事。每天为您展现最前沿产品信息和丰富的职场技巧。" />
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no" />
    <link rel="shortcut icon" href="{{asset "images/logo/logo.png"}}">
    {{! Styles'n'Scripts }}
    <link rel="stylesheet" type="text/css" href="{{asset "dist/css/index.min.css"}}" />
    <meta name="baidu-site-verification" content="DGzERzSCti" />
    <meta name="author" content="北京创仕科锐信息技术有限公司" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="360-site-verification" content="88aa861aac3a49c26b9c0739444a9cc2" />
    <meta name="sogou_site_verification" content="1iO9Pj2r8M"/>
    <script type='text/javascript'>
        var _vds = _vds || [];
        window._vds = _vds;
        (function(){
            _vds.push(['setAccountId', '8282882b138aee34']);
            (function() {
                var vds = document.createElement('script');
                vds.type='text/javascript';
                vds.async = true;
                vds.src = ('https:' == document.location.protocol ? 'https://' : 'http://') + 'dn-growing.qbox.me/vds.js';
                var s = document.getElementsByTagName('script')[0];
                s.parentNode.insertBefore(vds, s);
            })();
        })();

        var _hmt = _hmt || [];
        (function() {
          var hm = document.createElement("script");
          hm.src = "//hm.baidu.com/hm.js?b486bc02e446cec339e6c08eb84b5aec";
          var s = document.getElementsByTagName("script")[0];
          s.parentNode.insertBefore(hm, s);
        })();
        // 客服系统
        window.NTKF_PARAM = {
            siteid: 'kf_9458',               // 企业ID，为固定值，必填
            settingid: 'kf_9458_1479119226945',  // 接待组ID，为固定值，必填
            uid: '',                    // 用户ID，未登录可以为空，但不能给null，uid赋予的值显示到小能客户端上
            uname: '',        // 用户名，未登录可以为空，但不能给null，uname赋予的值显示到小能客户端上
            isvip: '0', // 是否为vip用户，0代表非会员，1代表会员，取值显示到小能客户端上
            userlevel: '1',                    // 网站自定义会员级别，0-N，可根据选择判断，取值显示到小能客户端上
            erpparam: 'abc'                      // erpparam为erp功能的扩展字段，可选，购买erp功能后用于erp功能集成
        };
    </script>
    <script src="//dl.ntalker.com/js/xn6/ntkfstat.js?siteid=kf_9458" async></script>

    {{!-- {{head-meta}} --}}

    {{! Ghost outputs important style and meta data with this tag }}
    {{ghost_head}}
</head>
<body class="{{body_class}} {{#is 'index, tag, author'}}user{{/is}} {{#is 'post'}}{{#unless post.image}}noimage{{/unless}}{{/is}}">
    {{> "header-nav"}}
    {{! Everything else gets inserted here }}
    {{{body}}}
    {{!-- {{#if @blog.logo}}
        <a href="{{@blog.url}}" id="blog_badge">
            <span style="background-image: url('{{@blog.logo}}');">{{@blog.title}}</span>
        </a>
    {{/if}} --}}

    {{!--
    <footer class="site-footer clearfix">
         <section class="copyright"><a href="{{@blog.url}}">{{@blog.title}}</a> &copy; {{date format="YYYY"}}</section>
         <section class="poweredby">Proudly published with <a href="https://ghost.org">Ghost</a> &amp; <a href="http://www.ghostchina.com">GhostChina</a></section>
    </footer>
    --}}
    <script>
    var ga = function () {};
        {{#is 'post'}}
            function get_text(el) {
                ret = "";
                var length = el.childNodes.length;
                for(var i = 0; i < length; i++) {
                    var node = el.childNodes[i];
                    if(node.nodeType != 8) {
                        ret += node.nodeType != 1 ? node.nodeValue : get_text(node);
                    }
                }
                return ret;
            }

        {{/is}}

        function no_schema_links () {
            var links = document.querySelectorAll('.js-remove-domain-schema');
            if (links) {
                for (i = 0; i < links.length; ++i) {
                    var link = links[i],
                        text = link.innerHTML,
                        no_schema = text.replace(/.*?:\/\//g, "");
                    link.innerHTML = no_schema;
                }
            }
        }

        window.onload = function () {
            no_schema_links();
        }
    </script>

    {{! Ghost outputs important scripts and data with this tag }}
    {{ghost_foot}}

    {{#is 'post'}}
        <script src = '//apps.bdimg.com/libs/jquery/1.6.4/jquery.min.js'></script>

        <script>
            $(function(){
                var share_modal = $("#share_modal"),
                    update_social_links = true;

                $("#btn_share").click(function(){
                    var that = $(this);
                    share_modal.fadeIn(200);
                    return false;
                });

                share_modal.click(function(e){
                    if (e.target.className == "wrap" || e.target.id == "share_modal") {
                        share_modal.fadeOut(200);
                    }
                    return false;
                });

                share_modal.find("div.inner > header > a.close").click(function(){
                    share_modal.fadeOut(200);
                    return false;
                });
            });
        </script>
    {{/is}}
    <script>
    var BASE_URL = '/task';
    </script>
    <script src = "{{asset "dist/js/all.min.js"}}"></script>
    {{> "loginDialog"}}
    <script type="text/javascript">
    $('.nav > .nav-blog > a').addClass('active');
    window.addEventListener('load', function () {
      var AVATAR_HAS = false;
      $.post('/task/login/authAjax')
       .success(function (data) {
            if (!data.success) return;
            var AVATAR_URL = "https://rishiqing-avatar.oss-cn-beijing.aliyuncs.com";
            if (data.hasAvatar) {
              AVATAR_URL = AVATAR_URL + '/_small_avatar' + data.id + '.png';
            } else {
              AVATAR_URL = 'https://rishiqing-images.oss-cn-beijing.aliyuncs.com/avatar/_small_avatar1.png'
            }
            $('.log-reg').hide();
            var $avatar = $('#logAvatar');
            if ($avatar && $avatar.length) {
              $avatar.find('.avatarImage').css('background-image','url("'+AVATAR_URL+'")');
              $avatar.find('.avatarName').text(data.realName);
              $avatar.addClass('showing').click(function () {
                location.href = '/app';
              });
            }
       });
    });
    </script>
</body>
</html>
